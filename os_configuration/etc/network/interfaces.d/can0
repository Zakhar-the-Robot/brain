# CANbus ===================================
# be sure that the networking service is up:
# sudo systemctl enable networking
# sudo systemctl start networking
auto can0
iface can0 can static
    bitrate 125000
